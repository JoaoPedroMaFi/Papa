Flask==3.0.3
Flask-SQLAlchemy==3.1.1
Flask-WTF==1.2.1
Flask-Reuploaded==1.4.0 # Or the latest stable version you prefer
Werkzeug==3.0.3 # Usually a dependency of Flask, but good to include if you use it directly
SQLAlchemy==2.0.29 # Usually a dependency of Flask-SQLAlchemy, but explicit is good
WTForms==3.1.2 # Usually a dependency of Flask-WTF, but explicit is good
gunicorn==23.0.0
